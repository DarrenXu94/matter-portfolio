---
const projects = [
  {
    title: "Test",
    description: "lorem ipsum",
    link: "a",
    github: "a",
  },
  {
    title: "Test",
    description: "lorem ipsum",
    link: "a",
    github: "a",
  },
  {
    title: "Test",
    description: "lorem ipsum",
    link: "a",
    github: "a",
  },
  {
    title: "Test",
    description: "lorem ipsum",
    link: "a",
    github: "a",
  },
];

function randomIntFromInterval(min: number, max: number) {
  // min and max included
  return Math.floor(Math.random() * (max - min + 1) + min);
}
---

<div class="wrapper">
  {
    projects.map((data, idx) => (
      <div
        class="card"
        style={{
          top: `${idx * 20}px`,
          transform: `rotate(${randomIntFromInterval(1, 3) * (idx % 2 ? -1 : 1)}deg)`,
        }}
      >
        <h2>{data.title}</h2>
        <p>{data.description}</p>
        <a href={data.link}>Link</a>
        <a href={data.github}>Github</a>
      </div>
    ))
  }
</div>

<style lang="scss">
  .wrapper {
    margin-top: 100px;
    padding-top: 150px;
    padding-bottom: 1000px;
    max-width: 700px;
    margin: 0 auto;
  }

  .card {
    top: 30px;
    position: sticky;
    border: 1px solid #ccc;
    box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.1);
    background-color: white;
    color: #333;
    padding: 40px;
    border-radius: 10px;
    margin-bottom: 20px;

    h2 {
      padding: 0;
    }
  }
</style>
